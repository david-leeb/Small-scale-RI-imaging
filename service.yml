apiVersion: v1
kind: Service
metadata:
  name: mrop-service
  labels:
    eidf/user: s2908448-aspp
spec:
  selector:
    app: mrop-jupyter
  ports:
  - protocol: TCP
    port: 8888
    targetPort: 8888
  type: ClusterIP